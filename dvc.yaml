stages:
  train:
    cmd: python -c "from pathlib import Path; Path('artifacts').mkdir(exist_ok=True);
      Path('artifacts/model.txt').write_text('week1')"
    deps:
    - data/raw
    - src/train.py
    outs:
    - artifacts/model.txt
